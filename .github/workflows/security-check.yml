name: Vharse DevOps Security Guard

on:
  push:
    branches: [main, ci-updates]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sast:
    uses: ./.github/workflows/sast.yml
    permissions:
      contents: read
      pull-requests: read
      security-events: write
      actions: read
      checks: write
    secrets:
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

  secret-scanning:
    uses: ./.github/workflows/secret-scanning.yml
    permissions:
      contents: read
      pull-requests: read
      security-events: write
      actions: read
      checks: write

  container-and-sbom:
    uses: ./.github/workflows/container-sbom.yml
    permissions:
      contents: read
      security-events: write
      actions: read
      checks: write
    secrets:
      DEFECTDOJO_URL: ${{ secrets.DEFECTDOJO_URL }}
      DEFECTDOJO_API_KEY: ${{ secrets.DEFECTDOJO_API_KEY }}

  iac-and-opa:
    uses: ./.github/workflows/iac-opa.yml
    permissions:
      contents: read
      security-events: write
      actions: read
      checks: write

  scorecard-audit:
    if: github.ref == 'refs/heads/main' || github.event_name == 'pull_request'
    uses: ./.github/workflows/scorecard.yml
    permissions:
      contents: read
      security-events: write
      actions: read
      checks: write
      id-token: write

  siem-telemetry:
    uses: ./.github/workflows/siem-telemetry.yml
    permissions:
      contents: read
      actions: read
    secrets:
      ELASTIC_HOST: ${{ secrets.ELASTIC_HOST }}
      ELASTIC_API_KEY: ${{ secrets.ELASTIC_API_KEY }}

  defectdojo-ingestion:
    needs: [sast, secret-scanning, container-and-sbom, iac-and-opa, scorecard-audit]
    if: always()
    uses: ./.github/workflows/defectdojo-sync.yml
    permissions:
      contents: read
      actions: read
    with:
      sast_job_needed: true
      container_job_needed: true
    secrets:
      DEFECTDOJO_URL: ${{ secrets.DEFECTDOJO_URL }}
      DEFECTDOJO_API_KEY: ${{ secrets.DEFECTDOJO_API_KEY }}
